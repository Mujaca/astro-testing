---
import { getCollection } from "astro:content";

const name = Astro.url.searchParams.get('name') || '';
let collection = await getCollection("files");

---
{
	collection.map((item) => (
		<article>
			<h2>{item.id}</h2>
			<p>{item.body}</p>
		</article>
	))
}